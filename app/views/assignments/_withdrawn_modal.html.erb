<div class="modal fade set-withdrawn-modal" id="withdrawnModal" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Withdrawn</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%= form_with url: assignment_set_withdrawn_path(@assignment), method: :patch, remote: false do |ff| %>
          <div class="form-group">
            <%= ff.label :reason %>
            <%= ff.text_field :reason, id: :reason, class:'form-control' %>
          </div>
          <div class="modal-footer">
            <%= ff.submit "Withdrawn", class:'btn btn-primary' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
