<%= form.fields_for(:assignments) do |f| %>
  <div class="form-group col">
    <%= f.label :status, "Status of #{f.object.vacancy.name} application, last updated at #{f.object.updated_at.to_date} " %>
    <%= f.text_field :status, class:'form-control' %>
  </div>
<% end %>
